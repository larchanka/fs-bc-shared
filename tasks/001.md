# Асинхронность в Javascript: Задача 1

### **Задача 1: Загрузка данных профиля пользователя (а-ля Twitter/X)**

### Решение задачи

```javascript
// Функция, имитирующая запрос информации о пользователе
function fetchUserInfo() {
  // Возвращаем Promise
  return new Promise(resolve => {
    // Имитируем задержку сети с помощью setTimeout
    setTimeout(() => {
      // Когда "ответ" готов, разрешаем Promise с обновленными данными пользователя
      console.log('Информация о пользователе получена');
      // Обновленные имя и bio
      resolve({ name: 'Алекс Алгоритмов', bio: 'Строю будущее, по одному циклу за раз.' });
    }, 1000); // Задержка 1 секунда
  });
}

// Функция, имитирующая запрос твитов пользователя
function fetchUserTweets() {
  // Возвращаем Promise
  return new Promise(resolve => {
    // Имитируем задержку сети
    setTimeout(() => {
      // Разрешаем Promise с обновленным массивом твитов
      console.log('Твиты получены');
      // Обновленные твиты
      resolve(['Коммичу в пятницу вечером. Что может пойти не так?', 'Баг или фича? 🤔 #программирование', 'Рефакторинг старого кода - это как археология.']);
    }, 1500); // Задержка 1.5 секунды
  });
}

// Функция, имитирующая запрос количества подписчиков
function fetchUserFollowers() {
  // Возвращаем Promise
  return new Promise(resolve => {
    // Имитируем задержку сети
    setTimeout(() => {
      // Разрешаем Promise с обновленным числом подписчиков
      console.log('Количество подписчиков получено');
      // Обновленное количество подписчиков
      resolve(15000);
    }, 500); // Задержка 0.5 секунды
  });
}

// Асинхронная функция для загрузки и отображения профиля
async function loadUserProfile() {
  console.log('Начинаем загрузку профиля...');
  try {
    // Используем await, чтобы дождаться выполнения каждого Promise
    // Запросы выполняются последовательно в данном случае
    const userInfo = await fetchUserInfo();
    const tweets = await fetchUserTweets();
    const followers = await fetchUserFollowers();

    // Выводим полученные данные в консоль
    console.log('\n--- Профиль пользователя ---');
    console.log(`Имя: ${userInfo.name}`);
    console.log(`Био: ${userInfo.bio}`);
    // Используем join для красивого вывода массива твитов
    console.log(`Твиты: ${tweets.join(', ')}`);
    console.log(`Подписчики: ${followers}`);
    console.log('--- Профиль успешно загружен! ---');

  } catch (error) {
    // Обработка возможных ошибок (хотя в этом примере их нет)
    console.error('Ошибка при загрузке профиля:', error);
  }
}

// Запускаем процесс загрузки профиля
loadUserProfile();
```
