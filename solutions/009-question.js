​//Пошаговое выполнение инструкций процессором, одна за другой.

let config = loadConfig();
let data = processData(config);
render(data);
console.log('Готово!');

//Что такое синхронный код?

​//Код, который выполняется строка за строкой, ожидая выполнение предыдущей строки.
